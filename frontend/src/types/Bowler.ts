// Defines the shape of a Bowler object used in the app
export type Bowler = {
    bowlerName: string;
    teamName: string;
    address: string;
    city: string;
    state: string;
    zip: string;
    phoneNumber: string;
};